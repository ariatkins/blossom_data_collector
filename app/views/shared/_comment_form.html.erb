<%= form_for(@comment) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <aside class="textarea">
    <%= f.text_area :contents, rows: "3", placeholder: "Write a new comment..." %>
  </aside>
  <%= f.submit "Save", class: "btn btn-medium btn-primary" %>
<% end %>